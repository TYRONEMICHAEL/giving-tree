{
  "name": "@giving-tree/prompts",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint './src/**/*.{ts,tsx}'",
    "build": "tsc && (cd src && find . -name '*.md' | cpio -pdm ../dist)"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "handlebars": "^4.7.8"
  }
}